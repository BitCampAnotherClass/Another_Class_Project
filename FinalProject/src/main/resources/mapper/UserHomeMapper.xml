<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anotherclass.bitcamp.dao.user.UserHomeDAO">
	<select id="popularCreator" resultType="com.anotherclass.bitcamp.vo.user.RegisterVO">
		<![CDATA[
		select creator_id, like_cnt 
		from (select creator_id, count(*) like_cnt from creatorlike 
		group by creator_id) 
		where rownum<=8 
		order by likecnt desc 
		]]>
	</select>

</mapper>